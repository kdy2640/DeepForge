1. 요청받지 않은 추상화나 범용화를 금지한다.
   공통 저장소, 제네릭 구조, 헬퍼, 파사드 변경, 자동 확장 구조를 임의로 추가하지 않는다.

2. 구조 변경이나 리팩터링 전에는 먼저 아래 내용을 보고하고 승인을 기다린다.
   - 변경할 파일
   - 추가·삭제할 타입과 메서드
   - 반드시 유지할 기존 동작
   - 불명확한 부분

3. 요구사항이 애매하면 구현하지 말고 한 가지 정확한 질문만 한다.
   추측해서 진행하지 않는다.

4. 에디터 조작이 필요할 시 MCP 연결을 확인하고 연결이 안 되어 있을시 요청한다.
   computer-use는 가급적 사용하지 않는다.

5. 검증이 3분 이상 걸리면 해당 검증은 중단한다.
   검증 범위를 줄이거나 더 빠른 방법으로 다시 시도한다.

6. 치명적이지 않은 TMP fallback 오류나 변경은 요청받지 않으면 수정하지 않는다.

7. 필수 참조에는 null 조건부 호출(`something?.Foo()`)을 사용하지 않는다.
   참조 누락을 조용히 무시하지 말고 직접 접근하여 문제가 즉시 드러나게 한다.
   요청받지 않은 null 검사, fallback, 예외 처리 등 과도한 방어 코드를 추가하지 않는다.

8. 대용량 YAML·diff·MCP 스키마 전체 출력을 금지하고 검색·수정·검증 범위를 필요한 부분으로 제한하며 MCP 검증은 원칙적으로 1회만 수행한다.

9. 개인 파일은 프로젝트 루트의 Assets 폴더와 같은 위계에 있는 Locals 폴더에 보관한다.
   Locals 폴더는 .git/info/exclude에 /Locals/ 항목을 추가하여 Git 추적 대상에서 제외한다.

10. 에디터 편의·디버깅만을 위한 코드는 기본적으로 추가하지 않는다.
    자동 복원, 스크립트 리로드 대응, 에디터 이벤트, 미리보기, 디버그 메뉴 등이 필요하다고 판단되면 목적·없을 때의 실제 문제·변경 범위를 설명하고 사용자에게 먼저 물어본 뒤 승인받아 추가한다.
    단순한 개발 편의를 필수 기능으로 취급하지 않는다. NativeArray.Dispose, Job 완료 대기, 메시 해제 등 실제 실행에 필요한 자원 정리는 유지한다.
